# this is a comment
# globals are only allowed in global/main (but not both)

# Value declarations (bad grammar intentional)
val a: [Int]
val b: [ String] = ["Hello", "World!"]
val c :<Int, Dec > = <2,3.14>
val d= {"key": "value", "otherKey": "otherValue"}
val e = {1, 2, 3, 4, 5, 5}

val x = 9
val y = "Hello, world"
val z = x

z = 12
